export type Reels = {
    firstReel: string,
    secondReel: string,
    thirdReel: string,
};

export type ReelImageProps = {
    reels: string[]
}

export type SpinerImageProps = {
    spin: boolean
}
